diff --git a/node_modules/three/build/three.module.js b/node_modules/three/build/three.module.js
index ad13abf..1e79033 100644
--- a/node_modules/three/build/three.module.js
+++ b/node_modules/three/build/three.module.js
@@ -16086,18 +16086,24 @@ class WebGLRenderer {
 		 * @default true
 		 */
 		this.isWebGLRenderer = true;
-
 		let _alpha;
 
 		if ( context !== null ) {
 
-			if ( typeof WebGLRenderingContext !== 'undefined' && context instanceof WebGLRenderingContext ) {
+			// PATCH(liteforms): expo-gl (React Native) provides a WebGL2-like
+			// context whose instance identifies as the global injected
+			// `WebGLRenderingContext` of Expo Go, and does not implement
+			// getContextAttributes() like a DOM context. Do not reject it as
+			// WebGL 1: read alpha attributes defensively instead.
+			if ( typeof context.getContextAttributes === 'function' ) {
 
-				throw new Error( 'THREE.WebGLRenderer: WebGL 1 is not supported since r163.' );
+				_alpha = context.getContextAttributes().alpha;
 
-			}
+			} else {
 
-			_alpha = context.getContextAttributes().alpha;
+				_alpha = alpha;
+
+			}
 
 		} else {
 
