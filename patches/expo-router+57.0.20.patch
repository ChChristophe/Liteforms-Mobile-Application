diff --git a/node_modules/expo-router/build/fork/useLinking.native.js b/node_modules/expo-router/build/fork/useLinking.native.js
index c080a58..8b1ee5b 100644
--- a/node_modules/expo-router/build/fork/useLinking.native.js
+++ b/node_modules/expo-router/build/fork/useLinking.native.js
@@ -124,7 +124,14 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
                         const state = getStateFromURL(url);
                         if (typeof url === 'string') {
                             // If the link were handled, it gets cleared in NavigationContainer
-                            onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
+                            // PATCH(liteforms): defer to the next task. On Android the initial
+                            // URL promise can settle while the NavigationContainer fiber is
+                            // still suspended (React.use on the thenable), and calling
+                            // `onUnhandledLinking` (a useState setter) then triggers React 19's
+                            // dev-only "state update on a component that hasn't mounted yet"
+                            // error. Deferring one task lets the container commit first.
+                            const patchedPath = (0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url);
+                            setTimeout(() => onUnhandledLinking(patchedPath), 0);
                         }
                         return state;
                     });
